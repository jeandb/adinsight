# Story 00.2: Integração Kiwify como canal de faturamento

**Status:** InReview
**Epic:** Legal & Integrations (Etapa 00 — Utilidades)
**Traces to:** Extensão da Etapa 8 — Integração WooCommerce / Canal de Faturamento

## User Story

Como gestora ou diretora, quero conectar a Kiwify como um canal de faturamento no AdInsight para que as vendas realizadas na plataforma apareçam no dashboard de receita junto com as lojas WooCommerce.

## Acceptance Criteria

- [x] Novo `source_type = 'kiwify'` suportado na tabela `woo_stores` (migration)
- [x] Novos campos de credencial para Kiwify: `kiwify_client_id_encrypted`, `kiwify_client_secret_encrypted`, `kiwify_account_id_encrypted` na tabela `woo_stores` (migration)
- [x] `kiwify.adapter.ts` implementado com autenticação OAuth2 (`client_id` + `client_secret`) e sincronização de pedidos via `account_id`
- [x] Endpoint `POST /api/woo-stores/:id/test-connection` funciona para lojas Kiwify
- [x] Endpoint `POST /api/woo-stores/:id/sync` sincroniza pedidos Kiwify para `woo_orders`
- [x] `WooSourceType` atualizado para `'woocommerce' | 'manual' | 'kiwify'` em `woo-stores.types.ts`
- [x] Tela Admin → Lojas exibe opção "Kiwify" no seletor de tipo de integração ao criar nova loja
- [x] Card Kiwify mostra os 3 campos de credencial: **Client ID**, **Client Secret**, **Account ID**
- [x] Card Kiwify exibe status de conexão, última sincronização e botões "Testar" / "Sincronizar"
- [x] Pedidos Kiwify aparecem no `/revenue` (tabela de pedidos e KPI cards) com o filtro de loja aplicável
- [x] `woo-stores.service.ts` roteia sync e test-connection para `kiwify.adapter.ts` quando `source_type = 'kiwify'`
- [x] Credenciais Kiwify criptografadas com AES-256 antes de salvar no banco (padrão existente)

## Scope

**IN:**
- Migration com novos campos de credencial Kiwify
- `kiwify.adapter.ts` em `apps/api/src/modules/woocommerce/`
- Atualização de `woo-stores.types.ts`, `woo-stores.service.ts`, `woo-stores.repository.ts`
- Atualização do tipo `WooSourceType` em `packages/shared-types`
- Atualização de `WooStoresPage.tsx` com card e campos Kiwify
- Atualização de `SaveCredentialsInput` para aceitar credenciais Kiwify

**OUT:**
- Sincronização de assinaturas/planos Kiwify (pode ser futura story)
- Webhooks Kiwify em tempo real
- Importação manual de Excel para lojas Kiwify

## Dependencies

- Story 00.1 (Done)
- Etapa 8 (Integração WooCommerce) — Done

## Technical Notes

- **Autenticação Kiwify:** OAuth2 client credentials flow
  ```
  POST https://api.kiwify.com.br/oauth/token
  { client_id, client_secret, grant_type: 'client_credentials' }
  → { access_token, expires_in }
  ```
- **Pedidos:** `GET https://api.kiwify.com.br/v1/orders?account_id={account_id}&page=1&limit=100`
- **Mapeamento de status Kiwify → WooOrderStatus:**
  - `paid` → `completed`
  - `waiting_payment` → `pending`
  - `refunded` → `refunded`
  - `chargedback` → `refunded`
  - `refused` → `failed`
- **Credenciais no banco:** usar prefixo `kiwify_` para distinguir dos campos WooCommerce existentes. Os campos `consumer_key_encrypted` e `consumer_secret_encrypted` permanecem para WooCommerce — não reutilizar.
- **Migration:** `20260422_000019_add_kiwify_credentials.sql`
- **Roteamento no service:** verificar `store.source_type` antes de chamar adapter:
  ```typescript
  if (store.source_type === 'kiwify') return kiwifyAdapter.sync(...)
  if (store.source_type === 'woocommerce') return wooAdapter.sync(...)
  ```
- **Frontend:** seguir exatamente o padrão do card WooCommerce em `WooStoresPage.tsx` — mesma estrutura visual, campos diferentes

## File List

_(atualizado por @build durante implementação)_

- `apps/api/src/shared/database/migrations/20260422_000019_add_kiwify_credentials.sql`
- `apps/api/src/modules/woocommerce/kiwify.adapter.ts`
- `apps/api/src/modules/woocommerce/woo-stores.types.ts`
- `apps/api/src/modules/woocommerce/woo-stores.service.ts`
- `apps/api/src/modules/woocommerce/woo-stores.repository.ts`
- `packages/shared-types/src/woocommerce.types.ts`
- `apps/web/src/features/admin/woo-stores/WooStoresPage.tsx`

## Change Log

| Date       | Agent  | Action                          |
|------------|--------|---------------------------------|
| 2026-04-22 | @own   | Created — status set to Ready   |
